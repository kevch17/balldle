{
  "name": "balldle",
  "version": "0.3.0",
  "private": true,
  "description": "A daily ball/strike calling game built on real Statcast pitches — prototype",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "npm run build && python3 -m http.server 8080 --directory dist",
    "test": "npm run build && npm run test:geometry && npm run test:break && npm run test:release && npm run test:alignment && npm run test:play",
    "test:geometry": "node tests/geometry.test.mjs",
    "test:break": "node tests/break.test.mjs",
    "test:release": "node tests/release.test.mjs",
    "test:alignment": "node tests/alignment.test.mjs",
    "test:play": "node tests/playthrough.test.mjs",
    "test:pipeline": "python3 tests/test_pipeline.py",
    "data:sample": "python3 scripts/gen_sample.py && npm run build"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  },
  "engines": { "node": ">=20" }
}
